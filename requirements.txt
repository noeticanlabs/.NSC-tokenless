fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
httpx>=0.25.0
Flask
numpy
scipy
matplotlib

# Optional ML dependencies for NPE Phase 2 (embeddings/predictor).
# The runtime will fall back to lightweight heuristics if these are absent.
tensorflow
scikit-learn